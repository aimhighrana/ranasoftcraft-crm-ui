
export interface Filters {
    textInput: string;
    modules: Array<object>;
    statuses: Array<object>;
    priorities: Array<object>;
    received_date: string;
    due_date: string;
    regions: Array<object>;
    requested_by: Array<object>
}