/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SchemaMappingListComponent } from './schema-mapping-list.component';

describe('SchemaMappingListComponent', () => {
  let component: SchemaMappingListComponent;
  let fixture: ComponentFixture<SchemaMappingListComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SchemaMappingListComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SchemaMappingListComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
